5.1.1
Нам могут быть предоставлены кандидаты в возрастющем порядке квалификации. Это будет значить, что мы можем применить транзитивность для того, чтобы определить разницу в уровне каждого из двух кандидатов.

5.1.2
n = lg_2(b − a + 1)
Создаем новый массив A длины n
for i = 1 to n do
	A[i] = RANDOM(0, 1)
//Возвращаем массив А, в котором запимано число между a и b в двоичной форме, переводим в дестичную
res = 0
power = 1
for i = 1 to n do
	sum += power * A[i]
	power *= 2
Теперь res содержит нужное число

5.1.3
res = 0;
while(a != b) {
	a = biased_random();
	b = biased_random();
	if (a > b)
		res = 1;
	if (b > a)
		res = 0;
}
Так как вероятность того, что результат будет 1 или 0 одинаков, то суммарно их вероятность будет 1/2;
количество итераций цикла, необходимых для достижения данного эффекта должно быть:
sum_j = 0^infty [j * (1 - 2p(1 - p))^j (2p(1 - p))] = (1 - 2p(1 - p))/(2p(p - 1))